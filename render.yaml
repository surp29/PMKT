services:
  - type: web
    name: phanmemketoan-backend
    env: python
    plan: free
    buildCommand: pip install -r PhanMemKeToan_backend/requirements.txt
    startCommand: cd PhanMemKeToan_backend && python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7  # ← Thay đổi từ 3.13.4 thành 3.11.7
      - key: DATABASE_URL
        fromDatabase:
          name: phanmemketoan-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://phanmemketoan.vercel.app,http://localhost:3000
      - key: ENVIRONMENT
        value: production

databases:
  # PostgreSQL Database
  - name: phanmemketoan-db
    databaseName: ketoan
    user: ketoan_user
    plan: free
